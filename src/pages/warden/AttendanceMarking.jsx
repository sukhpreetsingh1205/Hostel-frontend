// Similar to admin attendance marking but with warden-specific permissions
// Reuse the same component with role-based access
import AttendanceMarking from '../admin/Attendance/AttendanceMarking';
export default AttendanceMarking;